/*
	Implement an interface called "Discountable" that defines a method 
to calculate the discount on a product. Implement this interface in 
the "Product" class and override the method as needed for different types of products.

 */
public interface Discountable {
    public double discountCalc();
}
